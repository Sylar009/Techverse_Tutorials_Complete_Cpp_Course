#include <iostream>
using namespace std;

int main() {
    int a = 8, b = 0;

    // Logical AND
    cout << "a && b: " << (a && b) << endl;

    // Logical OR
    cout << "a || b: " << (a || b) << endl;

    // Logical NOT
    cout << "!a: " << (!a) << endl;

    return 0;
}