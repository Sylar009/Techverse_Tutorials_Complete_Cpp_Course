#include <iostream>
using namespace std;

int main() {
    int num = 10;  // Initialize variable

    if (num < 15) {  // Check if condition is true
        cout << "10 is less than 15 \n";
    }

    cout << "This statement is outside the if block.";
    return 0;
}