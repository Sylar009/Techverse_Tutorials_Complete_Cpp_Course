#include <iostream>
using namespace std;

int main() {
    int value = 10;  // Initialize variable

    if (value > 15) {  // Condition is false
        cout << "10 is greater than 15 \n";
    }

    cout << "Condition evaluated, exiting if block.";
    return 0;
}