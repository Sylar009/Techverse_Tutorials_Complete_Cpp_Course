#include <iostream>
using namespace std;

int main() {
    double decimal = 3.7; 

    // Explicit conversion to integer
    int wholeNumber = (int)decimal + 2;

    cout << "Whole Number = " << wholeNumber << endl;
    return 0;
}